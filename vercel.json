{
  "version": 2,
  "rootDirectory": "frontend",
  "build": {
    "command": "npm run build"
  },
  "install": {
    "command": "npm install"
  },
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "$BACKEND_API_URL/api/$1"
    }
  ]
}
